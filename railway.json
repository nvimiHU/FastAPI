{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  },
  "env": {
    "TELEGRAM_BOT_TOKEN": {
      "description": "توکن ربات تلگرام - از @BotFather دریافت کنید",
      "required": true
    },
    "TELEGRAM_ADMIN_IDS": {
      "description": "شناسه عددی ادمین‌های مجاز (با کاما جدا کنید)",
      "required": true
    },
    "VLESS_UUID": {
      "description": "UUID یکتا برای اتصالات WebSocket (اختیاری - در صورت خالی خودکار تولید می‌شود)",
      "required": false,
      "generator": "secret"
    },
    "XHTTP_TARGET_HOST": {
      "description": "آدرس مقصد برای ترابرد XHTTP (پیش‌فرض: localhost)",
      "required": false,
      "value": "localhost"
    },
    "XHTTP_TARGET_PORT": {
      "description": "پورت مقصد برای ترابرد XHTTP (پیش‌فرض: 443)",
      "required": false,
      "value": "443"
    }
  }
}
